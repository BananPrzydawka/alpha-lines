[project]
name = "alpha-lines"
version = "0.1.0"
description = "KLENT self-play training for alpha-lines"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "modal>=1.5.1",
    "torch>=2.12.1",
]
